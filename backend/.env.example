# Server
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Supabase (required for auth and data)
# Get from Supabase Dashboard → Settings → API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# JWT secret for auth tokens (use a long random string in production)
JWT_SECRET=your-secret-at-least-32-chars-long

# Clerk (JWT verification) – if using Clerk
CLERK_JWT_ISSUER=https://clerk.yourdomain.com
CLERK_JWT_AUDIENCE=your-audience

# Paystack (payments) – use test keys for development
PAYSTACK_SECRET_KEY=sk_test_xxx
PAYSTACK_WEBHOOK_SECRET=whsec_xxx
GEMINI_API_KEY=AIzaSyxxxx
GEMINI_MODEL=gemini-2.0-flash
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4o-mini
CLAUDE_API_KEY=sk-ant-xxx
CLAUDE_MODEL=claude-3-5-sonnet-latest

# Email (e.g. Gmail app password for forgot-password, etc.)
EMAIL_PROVIDER=gmail
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_FROM="Your App <your-email@gmail.com>"
